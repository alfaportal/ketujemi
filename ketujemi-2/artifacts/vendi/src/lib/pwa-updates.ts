/** PWA updates disabled on client — auto-reload during posting was wiping the listing form. */
export function setupPwaUpdates(): void {
  /* no-op */
}

export async function checkForAppUpdate(): Promise<void> {
  /* no-op */
}
