# ketujemi-2 — paketa e aplikacionit

Je brenda **pjesës së app-it** të monorepo-së KetuJemi.

- **Hap në Cursor:** folderi **prind** `..\` (`Documents\GitHub\ketujemi`), jo vetëm ky nën-folder.
- **Git / push / Railway:** gjithmonë nga repo root (`ketujemi/`), jo si projekt i veçantë.

Komanda: nga root, `pnpm dev` — ose nga këtu, `pnpm dev` (e njëjta gjë).
