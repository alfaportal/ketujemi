{
  "name": "ketujemi",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd ketujemi-2 && pnpm install --no-frozen-lockfile && pnpm --filter @workspace/vendi run build"
  }
}
